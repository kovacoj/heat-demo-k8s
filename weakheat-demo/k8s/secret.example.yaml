# Example only -- the real secret is created out-of-band with:
#   kubectl create secret generic weakheat-secrets -n kovacovsky-ns \
#     --from-literal=demo-token='<generated>' \
#     --from-literal=callback-token='<generated>'
apiVersion: v1
kind: Secret
metadata:
  name: weakheat-secrets
type: Opaque
stringData:
  demo-token: "REPLACE_ME"
  callback-token: "REPLACE_ME"
